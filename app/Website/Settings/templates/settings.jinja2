<html>

{% block content %}
<div class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-6 text-gray-800">Settings</h1>

    <div class="bg-white shadow-md rounded-lg p-6">
        <h2 class="text-xl font-semibold mb-4 text-gray-700">Prioritization Strategy</h2>
        <form method="POST">
            <div class="mb-4">
                <label class="block text-gray-700 text-sm font-bold mb-2">
                    Scoring Function
                </label>
                <div class="space-y-2">
                    <div class="flex items-center">
                        <input type="radio" id="logistic" name="scoring_strategy" value="logistic" {% if
                            settings.scoring_strategy=='logistic' %}checked{% endif %} class="mr-2">
                        <label for="logistic" class="text-gray-700">
                            <strong>Logistic Function (3-Day Spike):</strong> Priority increases exponentially as the
                            deadline approaches (T=72h). Best for "crisis mode" last-minute work.
                        </label>
                    </div>
                    <div class="flex items-center">
                        <input type="radio" id="exponential" name="scoring_strategy" value="exponential" {% if
                            settings.scoring_strategy=='exponential' %}checked{% endif %} class="mr-2">
                        <label for="exponential" class="text-gray-700">
                            <strong>Exponential Decay (Continuous Pressure):</strong> Priority increases smoothly and
                            continuously over time (T=168h). Best for steady, consistent work.
                        </label>
                    </div>
                </div>
            </div>

            <div class="flex items-center justify-end mt-6">
                <button type="submit"
                    class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">
                    Save Settings
                </button>
            </div>
        </form>
    </div>
</div>
{% endblock %}

</html>